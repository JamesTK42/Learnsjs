// Get today's date
let today = new Date();
let date = today.getDate() + '/' + (today.getMonth()+1);

let init = document.getElementById('sign_init').textContent;

// Print the message and the date
function codingSignOff() {
// alert("coded " + date + " h/e ");
alert(init);



}
